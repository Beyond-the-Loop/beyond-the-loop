apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bchat.serviceAccountName" . }}
  labels:
    {{- include "bchat.labels" . | nindent 4 }}
  annotations:
    iam.gke.io/gcp-service-account: {{ .Values.serviceAccount.gcpServiceAccount | quote }}
