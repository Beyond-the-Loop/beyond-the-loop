import { SHARED_INTRO } from './shared';

export const image_generation_prompt = `${SHARED_INTRO}

You are an image generation model. When the user requests an image, generate one. Keep any accompanying text short.`;
